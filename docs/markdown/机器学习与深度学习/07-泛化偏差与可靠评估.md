# 泛化、偏差与可靠评估：怎样证明模型真的“学会了”

## 本课目标

- 区分训练集、验证集和测试集；
- 识别欠拟合、过拟合、数据泄漏和分布变化；
- 根据任务选择比“总体准确率”更有意义的评估方式。

## 模型价值要在没见过的数据上体现

如果只检查练习过的题，就无法知道模型是在背答案，还是掌握了可以迁移的规律。**泛化**是模型对未见过、但与真实任务相关的数据保持良好表现的能力。

![欠拟合、合适拟合和过拟合的区别](./assets/generalization.svg)

### 欠拟合

模型太简单、特征不足或训练不充分，连训练数据中的主要规律都没有学到。训练与验证表现都差。

### 合适拟合

模型抓住主要规律，没有过度追随噪声。训练与验证表现合理，对新数据仍有效。

### 过拟合

模型在训练集上非常好，验证或真实环境中明显变差。它可能记住了背景、拍摄设备、标注习惯等偶然线索。

## 三类数据各自负责什么

| 数据集合 | 用途 | 类比 |
| --- | --- | --- |
| 训练集 | 调整模型参数 | 平时练习题 |
| 验证集 | 选择模型、超参数和停止时机 | 模拟测试 |
| 测试集 | 最终一次评价泛化能力 | 封存的正式考试 |

如果反复查看测试集并根据结果修改模型，测试集就已经参与开发，不能再提供独立评价。应重新准备真正未使用过的数据。

## 数据怎样划分才不会泄漏

随机划分并不总是安全：

- 同一个人的多张照片不能同时出现在训练集和测试集；
- 同一篇文章切出的段落不应跨集合；
- 预测未来时，测试数据应来自更晚时间；
- 同一设备或地点产生的高度相似记录要按组划分。

否则模型可能认出“人、文章、设备或背景”，而不是解决目标任务。

## 准确率之外还要看什么

以“发现有害内容”为例：

- **真正例：**有害内容被正确拦截；
- **假正例：**正常内容被误拦截；
- **假负例：**有害内容被漏过；
- **真负例：**正常内容被正确放行。

常见指标：

- **准确率：**所有样本中预测正确的比例；
- **精确率：**被模型判为正例的内容中，有多少真的为正例；
- **召回率：**所有真实正例中，有多少被模型找出；
- **F1：**在精确率和召回率之间做综合；
- **混淆矩阵：**直接展示各类错误数量。

选择指标必须结合错误代价。疾病筛查更怕漏诊，垃圾邮件系统也许更怕把重要邮件误删。没有脱离场景的“最佳指标”。

## 总体成绩可能掩盖群体差异

模型总体准确率 95%，并不保证每个群体都有 95%。如果某类口音、肤色、年龄、设备或地区在训练数据中很少，错误可能集中发生。

可靠评估应该：

- 按关键子群体分开报告；
- 关注最差表现而不只看平均数；
- 检查数据覆盖不足的场景；
- 允许受影响的人提出异议和申诉；
- 在上线后持续监测。

## 分布变化：现实不会永远停在训练那天

训练数据与使用时数据的规律发生变化，叫作分布变化。例如：

- 垃圾邮件写法更新；
- 新相机改变图片颜色；
- 新词语和表达方式出现；
- 用户群体或政策环境变化；
- 极端天气超出历史范围。

模型上线不是项目结束。需要监测输入变化、错误反馈与性能下降，并决定何时重新训练、限制使用或退回人工流程。

## 对抗“捷径学习”

模型会寻找能最快降低损失的线索，不保证那是人真正关心的原因。猫总在沙发上、狼照片总带雪地、医院病例总带某设备水印，都可能成为捷径。

可以通过以下方法发现：

1. 更换背景和拍摄条件；
2. 对关键特征做遮挡或反事实修改；
3. 使用来自不同地点和时间的外部测试集；
4. 分析错误样本而不只看平均指标；
5. 请领域专家检查模型依据是否合理。

## 可靠性检查清单

面对“我们的模型准确率达到 99%”，继续问：

1. 99% 是在哪个数据集上？
2. 测试集是否与训练集真正独立？
3. 类别比例是多少？
4. 哪一种错误最严重？
5. 各人群和环境表现是否一致？
6. 数据是否来自真实部署场景？
7. 模型置信度是否经过校准？
8. 上线后怎样监测和处理申诉？

## 检查理解

**题目：** 一个识别校园植物的模型，训练集和测试集都来自同一天、同一花坛的连续拍摄。测试准确率很高，最可能有什么问题？

**参考答案：** 图片背景、光线和拍摄角度高度相似，测试集缺乏独立性。模型可能利用花坛位置等捷径。应按地点、时间或植株分组，并使用不同环境的外部测试数据。

## 本课小结

- 泛化能力比训练集成绩更重要；
- 数据集划分要防止同源样本泄漏；
- 指标必须与错误代价、群体差异和真实环境相联系；
- 上线后的分布变化与监测属于模型生命周期的一部分。

## 参考资料

- [Google：Overfitting](https://developers.google.com/machine-learning/crash-course/overfitting/overfitting)——欠拟合、合适拟合和过拟合。
- [Google：Interpreting loss curves](https://developers.google.com/machine-learning/crash-course/overfitting/interpreting-loss-curves)——训练与验证曲线的判断。
- [Google：Classification metrics](https://developers.google.com/machine-learning/crash-course/classification/accuracy-precision-recall)——准确率、精确率和召回率。

